services:
  arweave:
    image: lucaarweave/arweave-node:0.0.4
    ports:
      - "1984:1984"
    environment:
      AR_RUNMODE: test
    volumes:
      - arweave_data:/mnt/arweave-data

  gateway:
    image: lucaarweave/arweave-gateway:0.0.1
    volumes:
      - gateway_data:/app
    links:
      - gateway_db
      - arweave
    ports:
      - "3000:3000"

  gateway_db:
    image: postgres:12
    volumes:
      - gateway_db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: arweave
      POSTGRES_PASSWORD: arweave
      POSTGRES_DB: arweave
    command: -p 5432

  bundler:
    image: pointnetwork/arweave-bundler:v1.0.0
    volumes:
      - $KEYSTORE/arweave.json:/app/keystore/key.json
    environment:
      AWS_HOST: https://test.com
      AWS_KEY: test
      AWS_SECRET: test

volumes:
  arweave_data:
  gateway_data:
  gateway_db_data:
