input {
  udp {
    ecs_compatibility => "v1"
    port => 12201
  }
}

filter {
  json {
    ecs_compatibility => "v1"
    source => "message"
  }
}

output {
  stdout {}
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
  }
}
