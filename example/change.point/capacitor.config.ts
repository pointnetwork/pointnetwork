import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'change.point',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
