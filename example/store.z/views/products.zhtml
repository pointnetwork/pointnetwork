{% extends 'layout.zhtml' %}

{% block contents %}

  {% set products = contract_get(host, 'Store', 'getProductsByStoreId', [storeId]) %}

    {% for product in products %}
        <div class="store">
            <h3 class="name">{{ product.name }}</h2>
            <p><i class="description">{{ product.description }}</i></p>
            <b class="price">{{ product.price }}</b>
        </div>
    {% endfor %}

{% endblock %}